# Odds Snapshots

Timestamped odds CSV snapshots are written here.

Default layout:

```text
data/odds_snapshots/
  latest.csv
  YYYY/
    MM/
      world_cup_first_round_odds_YYYYMMDDTHHMMSSZ.csv
```

The timestamp is UTC, so snapshots sort correctly by filename.
